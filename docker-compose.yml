services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: orders-products-dashboard

    ports:
      - '3000:3000'

    env_file:
      - .env.local

    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0

    restart: unless-stopped
